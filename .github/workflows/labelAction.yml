name: LabelAdder
run-name: ${{github.actor}}
on: 
  pull_request:
    types: ["open"]
job:
  job1:
    name: job1
    runs-on: ubuntu-latest
    steps:
      - name: step1
      - run: |
          echo "ssssssss"
